# Ghostty

Default terminal emulator for new machine setup.

## Setup

```bash
./apps/ghostty/ghostty.sh setup
```

Installs Ghostty with Homebrew and symlinks `config` to `~/.config/ghostty/config`.
