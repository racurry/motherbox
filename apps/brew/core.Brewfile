# Core formulae required by bootstrap setup steps.
# These are installed first so the dev environment is ready fast.
# Everything else (casks, dev tools, etc.) lives in the main Brewfile.

brew "asdf"                      # asdf.sh — runtime manager
brew "direnv"                    # direnv.sh
brew "fzf"                       # sourced in .zshrc
brew "jq"                        # claudecode.sh settings merge
brew "mas"                       # mas.sh — Mac App Store CLI
brew "pure"                      # zsh prompt theme
brew "zsh"                       # zsh.sh
brew "zsh-autosuggestions"       # .zshrc
brew "zsh-syntax-highlighting"   # .zshrc
