# Xcode Command Line Tools

Preflight check for Apple's command line developer tools.

## Setup

```bash
./apps/xcodecli/xcodecli.sh setup
```

Verifies `xcode-select` is configured or launches Apple's installer.
