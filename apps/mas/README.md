# Mac App Store

Mac App Store app installation through the `mas` CLI.

## Setup

```bash
./apps/mas/mas.sh setup
```

Installs common apps from `apps.txt` plus profile-specific apps from `{profile}.txt`.
