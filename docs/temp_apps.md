# Temp apps

Some apps that I don't really use very often.  Not installed by default anywhere, but it's nice to keep a list handy

```Brewfile
cask "chatgpt-atlas"
cask "comet"
cask "keyboard-maestro"
```
