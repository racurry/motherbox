# Project helper things ESLint

## ES Lint

No global configuration. ESLint is configured per-project by design.  To start a new project with ESLint, copy my opinionated default config, `eslint.config.js`, into the project roooooot.
